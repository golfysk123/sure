# JSP-000690 formalization research

The current full chromatic-statement development is in [`complete/`](complete/README.md).

The original `Prize690.lean` is retained as a historical finite certificate. Although its old CI passed, it did not formally connect mask enumeration to all colourings, represent simple hyperedges as finite sets, or prove arbitrary proper-subhypergraph criticality. Do not cite that earlier file as a complete proof of the original problem.

The new development supplies those missing obligations and documents its actual checks, scope, mathematical attribution and earlier competing submissions. It does not claim an award or first priority.

Project account: `golfysk123`. Awards PR #35 is owned by `superpilot69` and is not this project's submission.
